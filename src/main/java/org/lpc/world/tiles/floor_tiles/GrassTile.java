package org.lpc.world.tiles.floor_tiles;

import org.lpc.world.tiles.FloorTile;

public class GrassTile extends FloorTile {
    public GrassTile(int x, int y) {
        super(x, y, 1, 1, 0, 0, 1.0);
    }
}
