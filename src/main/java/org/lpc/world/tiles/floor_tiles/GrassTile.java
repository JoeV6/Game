package org.lpc.world.tiles.floor_tiles;

public class GrassTile extends FloorTile {
    public GrassTile(int x, int y, int width, int height, int textureID, double walkSpeedModifier) {
        super(x, y, width, height, textureID, walkSpeedModifier);
    }
}
